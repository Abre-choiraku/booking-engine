-- ============================================================
-- 枠の区切り（開始間隔）を所要時間と分離
-- ============================================================
-- slot_interval_min: 予約枠の開始間隔（分）。例: 所要90分でも間隔30分なら
--   10:00, 10:30, 11:00 … と開始でき、各開始から所要時間が空いていれば予約可。
--   null/0 = 従来どおり（所要時間 + buffer_min ごと）。
-- ============================================================

alter table booking_links add column if not exists slot_interval_min int;

notify pgrst, 'reload schema';
