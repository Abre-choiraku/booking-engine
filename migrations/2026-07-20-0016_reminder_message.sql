-- ============================================================
-- リマインドメールに差し込む任意の案内文（リンクごと）
-- ============================================================
-- 空/NULL のときは従来どおり自動テンプレートのみ。
-- 設定すると、リマインドメール本文の冒頭あいさつの後に差し込まれる。
-- ============================================================

alter table booking_links
  add column if not exists reminder_message text;

notify pgrst, 'reload schema';
