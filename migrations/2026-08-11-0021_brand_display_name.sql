-- ============================================================
-- リンク単位のブランド店名（VAILS連携: アカウント名を予約ページに表示）
-- ============================================================
-- パートナーAPI経由のリンクは全て同一オーナーに属するため、
-- オーナー単位の tenant_brands だけでは全ページが同じ屋号になる。
-- リンク単位で店名を上書きできる列を追加する（null = 従来どおりオーナーのブランド）。

alter table booking_links add column if not exists brand_display_name text;
